-- Initial snooker-statistics.cabal generated by cabal init.  For further 
-- documentation, see http://haskell.org/cabal/users-guide/

name:                snooker-statistics
version:             0.1.0.0
-- synopsis:            
-- description:         
homepage:            https://github.com/sleepomeno/snooker-statistics
license:             Apache-2.0
license-file:        LICENSE
author:              Gregor Riegler
maintainer:          gregor.riegler@gmail.com
-- copyright:           
category:            Web
build-type:          Simple
extra-source-files:  README.md
cabal-version:       >=1.10
Data-dir:            .
Data-files:          data/config.txt

flag build
   description: Use config.txt from share
   default: False

-- executable Manipulator
--   main-is:             src/Manipulator.hs
--   build-depends: old-locale >= 1.0.0.6, time >= 1.4.2, parsec, filepath >= 1.3.0.2, MissingH >= 1.2.1.0, base >= 4.7, tagsoup >= 0.13, webdriver == 0.6.2.1, transformers, text, mtl, ConfigFile >= 1.1, xml-conduit, parallel, deepseq, pretty-show, persistent, persistent-sqlite, persistent-template, errors, resourcet, monad-logger, either, aeson, bytestring

-- library
--   build-depends: old-locale >= 1.0.0.6, time >= 1.4.2, parsec, filepath >= 1.3.0.2, MissingH >= 1.2.1.0, base >= 4.7, tagsoup >= 0.13, webdriver == 0.6.2.1, transformers, text, mtl, ConfigFile >= 1.1, xml-conduit, parallel, deepseq, pretty-show, persistent, persistent-sqlite, persistent-template, errors, resourcet, monad-logger, either, aeson, bytestring, blaze-html

executable SnookFetcher
  main-is:             Fetcher.hs
  build-depends: old-locale >= 1.0.0.6, time >= 1.4.2, parsec, filepath >= 1.3.0.2, MissingH >= 1.2.1.0, base >= 4.7, tagsoup >= 0.13, webdriver == 0.6.2.1, transformers, text, mtl, ConfigFile >= 1.1, xml-conduit, parallel, deepseq, pretty-show, persistent, persistent-sqlite, persistent-template, errors, resourcet, monad-logger, either, aeson, bytestring, blaze-html


  default-language:    Haskell2010
  hs-source-dirs:      src
  if flag(build)
    cpp-options: -DCABAL
  -- extensions: GeneralizedNewtypeDeriving

executable SnookMarkdown
  main-is:             Markdown.hs
  build-depends: old-locale >= 1.0.0.6, time >= 1.4.2, parsec, filepath >= 1.3.0.2, MissingH >= 1.2.1.0, base >= 4.7, tagsoup >= 0.13, webdriver == 0.6.2.1, transformers, text, mtl, ConfigFile >= 1.1, xml-conduit, parallel, deepseq, pretty-show, persistent, persistent-sqlite, persistent-template, errors, resourcet, monad-logger, either, aeson, bytestring, blaze-html, foldl >= 1.0.10, containers, pipes, Chart, Chart-cairo
  default-language:    Haskell2010
  hs-source-dirs:      src
  if flag(build)
    cpp-options: -DCABAL

-- Test-suite test-snooker
--     Hs-Source-Dirs:       src,test
--     Main-Is:              Spec.hs
--     Type:                 exitcode-stdio-1.0

--     Build-Depends: old-locale >= 1.0.0.6, time >= 1.4.2, parsec, filepath >= 1.3.0.2, MissingH >= 1.2.1.0, base >= 4.7, tagsoup >= 0.13, webdriver == 0.6.2.1, transformers, text, mtl, ConfigFile >= 1.1, xml-conduit, parallel, deepseq, pretty-show, persistent, persistent-sqlite, persistent-template, errors, resourcet, monad-logger, either

--     Ghc-Options:          -Wall

--     Build-Depends:        hspec >= 1.10.0.1, QuickCheck >= 2.7.6
--     default-language:    Haskell2010
